- name: task level variables
  hosts: local
  connection: local
  vars:
   course: ansible
  tasks:
  - name: printing variables
    vars:
     course: ansible-new
    ansible.builtin.debug:
     msg: "Course name is {{ course }}"